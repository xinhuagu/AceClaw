// aceclaw-mcp: MCP protocol client/server — stdio and SSE transports

dependencies {
    implementation(project(":aceclaw-core"))

    implementation("com.fasterxml.jackson.core:jackson-databind")
    implementation("org.slf4j:slf4j-api")
}
